package com.utdmod.network;

import net.fabricmc.fabric.api.networking.v1.PayloadTypeRegistry;
import net.fabricmc.fabric.api.networking.v1.ServerPlayNetworking;

public class ModNetworkHandler {
public static void register() {
// Register packet type
PayloadTypeRegistry.playS2C().register(TensionSyncPayload.ID, TensionSyncPayload.CODEC);

    // Server will send using: ServerPlayNetworking.send(player, new TensionSyncPayload(...))
}


}
